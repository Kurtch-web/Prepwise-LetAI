[project]
name = "prepwise-backend"
version = "0.1.0"
description = "Prepwise Backend - Educational Platform"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[tool.uv]
dev-dependencies = []
